import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminVendorsPage() {
  return <PlaceholderPage eyebrow="Admin" title="Vendor approvals" description="Demo module for vendor verification and public listing approvals." modules={["Applications", "Documents", "Location consent", "Products", "Approval status", "Audit log"]} />;
}
