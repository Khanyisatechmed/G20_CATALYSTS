import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminAnalyticsPage() {
  return <PlaceholderPage eyebrow="Admin" title="Platform analytics" description="Demo analytics shell. Production analytics must use real consented data." modules={["Traffic", "Bookings", "Orders", "Regions", "Vendors", "Content"]} />;
}
