import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminSettingsPage() {
  return <PlaceholderPage eyebrow="Admin" title="Platform settings" description="Demo configuration shell for integrations and permissions." modules={["Permissions", "Payments", "Maps", "AI", "Storage", "Monitoring"]} />;
}
