import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminEventsPage() {
  return <PlaceholderPage eyebrow="Admin" title="Event management" description="Demo module for verified event creation and moderation." modules={["Draft events", "Dates", "Organisers", "Province", "Publication", "Audit log"]} />;
}
